# Environment variables are not required for production deployment since this is a static client-side only app.
# The user provides their own Gemini API key in the UI.

VITE_APP_TITLE=Storyboard Prompt Builder